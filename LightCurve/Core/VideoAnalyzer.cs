using System.IO;

namespace LightCurve.Core
{
    internal class VideoAnalyzer(
        List<FileInfo> files,
        int channel,
        uint? x,
        uint? y,
        uint? w,
        uint? h,
        int outputType,
        string outputPath)
    {
        internal void Run()
        {

        }
    }
}
